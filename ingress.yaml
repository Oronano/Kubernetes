apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-ingress
  namespace: devs
spec:
  rules:
    - http:
        paths:
          - path: /app/
            pathType: Prefix
            backend:
              service:
                name: ingress-service
                port:
                  number: 80
